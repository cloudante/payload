import React from 'react'

// This component can be used to extend the default admin UI for the LeadGenPages collection
const LeadGenPagesAdmin: React.FC = () => {
  return (
    <div className="custom-lead-gen-admin">
      {/* Custom admin UI components can be added here when needed */}
    </div>
  )
}

export default LeadGenPagesAdmin
